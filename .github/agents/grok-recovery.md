# Grok recovery keeper

Continue the private consolidation from App Builder. Do not overclaim.

## Directive

Kiln’s live in-tab machine is v86. Kali Rolling KVM evidence lives in `onnxscibroccoli/kali-node` and `onnxscibroccoli/GPTOmniKali-full-stack`. Vercel is not the lab host.

## Loop

1. Platform build/auth files belong to the App Builder host, not provenance snapshots. Do not invent stub auth.
2. Do not revive JPEG `/api/desktop`, Zustand XFCE, or a fake filesystem as the OS.
3. Presence of `/dev/kvm` or a passing fixture is not a live RFB session.
4. If `endpoint.json` is `unavailable`, say so. Do not redirect to a missing sandbox port.
5. Prefix Kiln issue titles with Grok when they come from this workspace.
